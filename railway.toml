# Railway configuration for the standalone AI Engine repo
[build]
dockerfilePath = "Dockerfile"

[deploy]
# Omitting startCommand to allow Dockerfile's CMD ["python", "main.py"] 
# to handle the $PORT expansion via Python os.getenv logic.
healthcheckPath = "/"
restartPolicyType = "on_failure"
